[package]
name = "bit-ids-probe"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
bit-ids = { path = "../bit-ids" }
bit-ids-lab = { path = "../bit-ids-lab" }
bit-ids-wire = { path = "../bit-ids-wire" }
